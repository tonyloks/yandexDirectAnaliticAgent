---
description: 
globs: 
alwaysApply: true
---
Правила написания кода и ответов для AI-агента

1. Пиши кратко, объясняй концепции Твоя главная задача — экономить мое время. Код должен быть лаконичным, а объяснения — четкими и по делу, фокусируясь на ключевых решениях.

   ❌ Неправильно (слишком многословно):

   "Итак, для решения вашей задачи я собираюсь реализовать функцию. Эта функция будет принимать один аргумент, который, как я предполагаю, будет списком целых чисел. Затем я инициализирую переменную для хранения суммы, изначально присвоив ей ноль. После этого я пройдусь в цикле по каждому элементу входного списка и на каждой итерации буду прибавлять текущий элемент к переменной суммы. В конце, после завершения цикла, функция вернет итоговое значение этой переменной."

   ✅ Правильно (кратко и по делу):

   "Вот функция для суммирования элементов списка. Я использую встроенную функцию sum() для максимальной эффективности и читаемости кода."

   ```python
   def get_total_sum(numbers: list[int]) -> int:
       """Возвращает сумму всех чисел в списке."""
       return sum(numbers)
   ```

2. Используй полную семантическую разметку Каждый фрагмент кода должен быть интуитивно понятен без необходимости его запуска. Цель — максимальная ясность для человека и других AI-инструментов.

   2.1. Контракт функции (Сигнатура) 👉 Правило: Строго аннотируй типы и используй описательные имена.

   ❌ Неправильно:

   ```python
   # обработка данных
   def proc(d):
       # ...
   ```

   ✅ Правильно:

   ```python
   def filter_active_users(users: list[dict]) -> list[dict]:
       # ...
   ```

   2.2. Намерение и логика (Документация и комментарии) 👉 Правило: Пиши исчерпывающие docstrings и объясняй "почему", а не "что".

   ❌ Неправильно:

   ```python
   def calculate_discount(price, discount):
       # Умножаем цену на скидку
       return price * discount
   ```

   ✅ Правильно:

   ```python
   def calculate_discounted_price(original_price: float, discount_percentage: int) -> float:
       """
       Вычисляет итоговую цену товара после применения скидки.

       Args:
           original_price: Исходная цена товара.
           discount_percentage: Скидка в процентах (например, 20 для 20%).

       Returns:
           Цена товара после вычета скидки.
       """
       # Бизнес-логика: скидка применяется как доля от цены,
       # поэтому проценты необходимо перевести в десятичный формат.
       discount_factor = discount_percentage / 100
       return original_price * (1 - discount_factor)
   ```

   2.3. "Магические" значения 👉 Правило: Выноси любые повторяющиеся или имеющие бизнес-смысл значения в константы.

   ❌ Неправильно:

   ```python
   def get_db_connection():
       # Подключаемся к базе данных
       return create_connection("127.0.0.1", 5432, timeout=10)
   ```

   ✅ Правильно:

   ```python
   # config.py
   DB_HOST = "127.0.0.1"
   DB_PORT = 5432
   DEFAULT_TIMEOUT_SECONDS = 10

   def get_db_connection():
       """Создает и возвращает соединение с базой данных, используя параметры из конфигурации."""
       return create_connection(DB_HOST, DB_PORT, timeout=DEFAULT_TIMEOUT_SECONDS)
   ```

3. Предпочитай структурированные данные Вместо общих типов (dict), всегда определяй явную структуру для сложных сущностей (dataclasses, Pydantic).

   ❌ Неправильно (семантически бедно):

   ```python
   def get_user_info(user: dict) -> str:
       # Непонятно, какие ключи есть в словаре 'user'
       return f"Имя: {user['name']}, Возраст: {user['age']}"
   ```

   ✅ Правильно (семантически богато):

   ```python
   from dataclasses import dataclass

   @dataclass
   class User:
       """Представляет модель пользователя в системе."""
       id: int
       name: str
       age: int

   def get_user_info(user: User) -> str:
       """Возвращает форматированную строку с информацией о пользователе."""
       return f"Имя: {user.name}, Возраст: {user.age}"
   ```

4. Пиши тестируемый и безопасный код Отделяй логику от конфигурации и явно указывай на побочные эффекты.

   ❌ Неправильно (логика и конфигурация смешаны):

   ```python
   def save_user_data(user: User):
       # Путь "зашит" прямо в коде, функцию сложно тестировать
       with open("C:/data/prod/users.txt", "a") as f:
           f.write(str(user))
   ```

   ✅ Правильно (конфигурация вынесена, побочный эффект ясен):

   ```python
   from pathlib import Path

   def save_user_data(user: User, output_path: Path) -> None:
       """
       Сохраняет данные пользователя в указанный файл.

       Side Effects:
           - Создает или дописывает данные в файл по пути 'output_path'.

       Args:
           user: Объект пользователя для сохранения.
           output_path: Путь к файлу для записи.
       """
       with output_path.open("a", encoding="utf-8") as f:
           f.write(str(user))
   ```

5. Добавляй комментарии СВЕРХУ каждого .py файла 👉 Правило: Для каждого Python-файла обеспечивай краткое описание его назначения и основных компонентов в виде комментариев в начале файла.

6. При изменении кода ВСЕГДА перепроверяй и при необходимости изменяй:
 - **Докстринги и документацию**
 - **Комментарии в коде и файле в целом**
 - **Модели данных**


